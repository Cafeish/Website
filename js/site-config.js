/* =========================================================
   SITE CONFIG
   =========================================================
   Password for the /admin.html page (menu items, team, theme).
   Change this to whatever you want before sharing admin.html
   with your team. This is basic — not real security — it just
   keeps casual visitors out.
   ========================================================= */
const ADMIN_PASSWORD = "#7CafeishByAisha";

/* =========================================================
   EMAIL NOTIFICATIONS (Web3Forms — free, no card, no account)
   =========================================================
   This is what makes waitlist signups and reviews actually land
   in your inbox instead of only saving in the visitor's browser.

   1. Go to https://web3forms.com
   2. Enter cafeish.team@gmail.com and click "Create Access Key"
   3. Check that inbox — Web3Forms emails you a key that looks like
      a long string of letters/numbers
   4. Paste it below, replacing "YOUR_ACCESS_KEY"
   5. Save this file, refresh the site

   Until this is filled in, forms still save locally (same as
   before) but nothing gets emailed to the team.
   ========================================================= */
const WEB3FORMS_ACCESS_KEY = "b70c3a35-2b74-4576-96fe-fc753722ef7e";
const NOTIFY_EMAIL = "cafeish.team@gmail.com";
