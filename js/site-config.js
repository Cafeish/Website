/* =========================================================
   SITE CONFIG
   =========================================================
   Password for the /admin.html page (menu items, team, theme).
   Change this to whatever you want before sharing admin.html
   with your team. This is basic — not real security — it just
   keeps casual visitors out.
   ========================================================= */
const ADMIN_PASSWORD = "cafeish2026";
